interface:
  display_name: "No Yak Shaving"
  short_description: "Ship simple code and only valuable tests"
  default_prompt: "Use $no-yak-shaving to complete this change with only the complexity and validation the requirement needs."
