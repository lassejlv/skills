interface:
  display_name: "Use Goal"
  short_description: "Finish durable goals with metadata, memory, and history"
  default_prompt: "Use $use-goal to persist this objective with harness-readable metadata, local memory, and previous-goal history, then keep working until every acceptance criterion is verified."
