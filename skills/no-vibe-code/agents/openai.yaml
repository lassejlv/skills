interface:
  display_name: "No Vibe Code"
  short_description: "Catch generated-default frontend design tells"
  default_prompt: "Use $no-vibe-code to review this frontend for generic design patterns, triage lint findings against the brief, and inspect the rendered UI."
